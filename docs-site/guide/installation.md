# Installation

## Core runtime

```bash
npm install a2a-mesh
```

## Common stacks

```bash
npm install a2a-mesh a2a-mesh-adapters
```

## Optional companion tools

```bash
npm install a2a-mesh-registry a2a-mesh-cli
```

## Requirements

- Node.js 22.13 or newer
- TypeScript 5.x for the best DX
- optional provider SDKs such as `openai`, `@anthropic-ai/sdk`, or `langchain`
