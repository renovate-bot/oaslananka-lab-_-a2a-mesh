# Changelog

The release log is maintained in the repository root `CHANGELOG.md`.

Public releases are cut with release-please manifest mode, and docs deployments are handled by the
GitHub Pages workflow.
