name: Release PR (Disabled)

# Releases are managed by Azure DevOps.
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type CONFIRM to proceed'
        required: true

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release is managed by Azure DevOps pipelines."
