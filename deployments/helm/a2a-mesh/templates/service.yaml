apiVersion: v1
kind: Service
metadata:
  name: a2a-mesh
  namespace: '{{ .Release.Namespace }}'
spec:
  type: '{{ .Values.service.type }}'
  selector:
    app: a2a-mesh
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 3000
