# Azure DevOps is a manual verification target for this repository.
# The source of truth is the GitHub organization repository. Keep Azure runs
# explicit so a source push cannot start duplicate CI automatically.
trigger: none
pr: none

jobs:
  - template: pipelines/templates/ci-job.yml
    parameters:
      nodeVersion: '24.14.1'
