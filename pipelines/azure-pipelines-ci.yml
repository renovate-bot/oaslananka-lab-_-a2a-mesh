# Azure DevOps is a manual verification target. Automated CI/CD runs from
# the GitHub organization mirror so source pushes do not start duplicate
# Azure jobs.
trigger: none
pr: none

jobs:
  - template: templates/ci-job.yml
    parameters:
      nodeVersion: '24.14.1'
